// Clean up test data from branch 1 so editor shows defaults
\App\Models\BranchLandingSetting::where('branch_id', 1)
    ->whereIn('key', ['pricing_cards', 'branch_testimonials'])
    ->delete();
echo "Cleaned test data from branch 1\n";
// Verify clean
$s = \App\Models\BranchLandingSetting::forBranch(1);
echo "pricing_cards: " . ($s['pricing_cards'] ?? 'EMPTY (correct)') . "\n";
echo "branch_testimonials: " . ($s['branch_testimonials'] ?? 'EMPTY (correct)') . "\n";
