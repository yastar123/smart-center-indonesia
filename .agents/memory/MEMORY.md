- [Route ordering bug](route-ordering.md) — static routes must come before wildcard `{param}` routes in the same prefix group or Laravel matches the wildcard first.
- [Course & Grade model fields](course-grade-fields.md) — Course uses `cabang_id`/`nama` (not branch_id/name); Grade uses `siswa_id`/`mata_pelajaran_id`/`jenis_penilaian`/`nilai` (not student_id/course_id/jenis); SchoolClass uses `nama_kelas` (not nama), `cabang_id`.
- [Blade push-scripts PHP scope](blade-push-scope.md) — PHP functions defined in `@push('scripts')` are NOT available in `@section('content')` which renders first; define helper functions in a `@php` block at the TOP of the file before `@section`.
- [Profile page stack mismatch](profile-stack.md) — profile/edit and auth views used `<x-app-layout>` (Breeze Tailwind) which is incompatible with the Bootstrap app layout; rewritten as plain Bootstrap + `@extends('layouts.app')`.
- [Owner dashboard fake data](owner-dashboard-fake.md) — original owner dashboard had hardcoded numbers (12, 1240, 85, Rp120JT); replaced with live Eloquent queries in the view.
- [Payment/Schedule controllers](payment-schedule-controllers.md) — Invoice CRUD uses route model binding on `Invoice $payment` (matches `{payment}` param), markPaid route uses `{invoice}`. SchoolClass table is `school_classes`, field for name is `nama`.
- [DashboardService revenue](dashboard-revenue.md) — revenue was hardcoded 0; now queries Payment::where('status','verified')->sum('jumlah'), scoped by branch_id for admin.
- [Demo data seeding](demo-data-seeding.md) — DemoDataSeeder pitfalls: teachers.user_id was missing, admin.branch_id was NULL, Branch fillable excludes user_id/admin_id.
- [Revenue date field](revenue-date-field.md) — Payment monthly revenue must filter on `tanggal_pembayaran` not `created_at`; seeded payments have tanggal_pembayaran set to historical dates, not created_at.
- [Dashboard redirects](dashboard-redirects.md) — guru/siswa must be redirected from DashboardController before the match expression; they have dedicated portals at guru.dashboard and siswa.dashboard routes.
- [Bootstrap primary override](bootstrap-primary.md) — `--bs-primary` set to `#c84ddf` in :root so `.text-primary`, `.btn-primary`, `.btn-outline-primary` auto-render purple; no manual replace needed.
- [Mobile bottom nav role links](mobile-nav-roles.md) — guru mobile nav had wrong `admin.schedules.index` link; corrected to `guru.attendance`. Mobile nav font-size bumped 9.5px→11px with min tap target 44×44px.
- [Toast signature & flash toast pitfalls](toast-pitfalls.md) — showToast(msg, type) — message first, type second. Flash JSON must NOT fall back to session('status') or raw codes like 'profile-updated' show as toasts.
- [Invoice status values](invoice-status.md) — Invoice statuses are `belum_bayar`/`sebagian`/`lunas` (not 'unpaid'/'paid'). Count-up elements with data-target animate; those without data-target render static.
- [Sidebar orphan file](sidebar-orphan.md) — `resources/views/layouts/sidebar.blade.php` is never @include'd; real sidebar is inline in `layouts/app.blade.php` lines 815–1041. Also `resources/views/admin.blade.php` and `formdaftarsiswa.blade.php` are orphaned legacy files with no routes.
- [Attendance & grades AJAX contract](attendance-grades-ajax.md) — Both controllers return JSON (AJAX-only). Attendance store expects `jadwal_id`+`absensi[i][siswa_id/status]`; form uses `schedule_id`+`attendance[sid]`. Status `alpha` (form) must map to `alpa` (controller). Grades has a new `storeBatch` endpoint at `guru.grades.storeBatch` for bulk `grades[sid]=nilai` form data.
- [Table header sidebar-hover bug](table-header-hover.md) — Using `background:var(--sidebar-hover)` on thead tr makes headers dark purple (#461256) in light mode with unreadable text. Use `var(--input-bg)` + `color:var(--text-muted)` instead. Row hover should use `rgba(104,17,126,.05)` not the variable.
- [Stat icon CSS classes](stat-icon-classes.md) — Use `bg-primary-soft/bg-success-soft/bg-warning-soft/bg-danger-soft/bg-info-soft` with `style="color:white"` for all stat-card icons. Never use `background:#hex22;color:#hex` pattern — it produces inconsistent soft colors vs gradient icons.
- [Brand color audit](brand-color-audit.md) — All structural page headers must use `linear-gradient(135deg,#260632 0%,#461256 50%,#c84ddf 100%)`. Intentional exceptions: semantic green (lunas/selesai), semantic red (danger), brand gold (`#f6af23`/`#e09000`) for owner quickdash, teal for videocall/messages, sky blue for `bg-info-soft`/izin attendance.
- [Fetch error handling pattern](fetch-error-pattern.md) — All fetch() chains must use `.then(r => { if (!r.ok) throw new Error('HTTP '+r.status); return r.json(); })` before the data handler; edit modals need `.catch(()=>showToast(...,'error'))`; delete/loadData need the same. Teachers uses jQuery $.ajax with `error:` callback instead. Guru views use async/await with try/catch/finally.
- [Soft-color CSS variables](soft-color-vars.md) — `--soft-primary/success/warning/info/danger/muted-bg/border/text` defined in `:root` and `[data-theme="dark"]` in layouts/app.blade.php. Use these instead of hardcoded hex for all inline badges, status chips, summary boxes. PHP statusMap arrays must use `var(--soft-*)` not hex. JS template literals also accept `var(--soft-*)` as inline style values. JS statusMaps that build innerHTML strings must use rgba() not hex for theme compatibility.
- [Teacher model fields](teacher-model-fields.md) — Teacher uses `nig` (not `nip`), `subjects` (JSON array cast, not `bidang_studi`), `name`, `gender`, `email`, `photo`, `branch_id`, `status`. Always `implode(', ', $teacher->subjects)` since it's cast to array.
- [Auto count-up: data-auto-count pattern](auto-countup.md) — Global auto count-up uses `data-auto-count` attribute (NOT `.count-up` class) to avoid conflicting with per-page IOs that watch `.count-up[data-target]`. Per-page IOs (like dashboard.blade.php) must remain independent.
- [Notification panel role route](notification-role-route.md) — Notification panel JS "view all" link must use a PHP `@php`-computed `$announcementsRoute` variable (empty string for non-admin roles) injected via `@json` into the script block; never hardcode `admin.announcements.index` in a shared JS template.
- [Empty migration bodies — systemic](empty-migrations.md) — 9 tables were created with only `id`+`timestamps`: packages, salaries, tryouts, tryout_attempts, modules, absensi_siswas, absensi_gurus, questions, grades. Fixed via `2026_06_09_200000_add_missing_columns_to_all_tables`. Always verify column count after migrate.
- [Invoice/Certificate relation names](invoice-cert-relations.md) — Invoice uses `siswa()` relation (not `student()`), FK is `siswa_id`. Certificate also uses `siswa()` and `cabang()`. Never call `->with('student')` on Invoice.
